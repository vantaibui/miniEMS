import { APP_PREFIX } from '@libs/constants';

export const AUTH_TOKEN_KEY = `${APP_PREFIX}auth_token`;
export const AUTH_REFRESH_TOKEN_KEY = `${APP_PREFIX}auth__refresh_token`;
export const AUTH_USER_INFO = `${APP_PREFIX}user_info`;
